/*
 * PostsPage Messages
 *
 * This contains all the text for the PostsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PostsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PostsPage container!',
  },
});
